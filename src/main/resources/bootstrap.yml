spring:
  application:
    name: courses-projects
  cloud:
    config:
      uri: http://Eureka-config-server-env.xmgppsbng2.us-east-2.elasticbeanstalk.com/  #where the config-service is running
      fail-fast: true             #the service will not run if it can't reach the config-service